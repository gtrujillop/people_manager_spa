(function () {
  var session = angular.module('learningSystem.session');
  session.factory('sessionFactory', [function () {

    var factory = {
      session: {}
    };

    return factory;
  }]);
})();